# ── 환경변수·시크릿 (절대 커밋 금지) ──
.env
*.pem

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── Django ──
*.log
db.sqlite3
db.sqlite3-journal
/media/
/staticfiles/

# ── Docker ──
# (docker-compose.override.yml 등 개인 설정이 있으면 여기에)

# ── IDE / OS ──
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── 마이그레이션은 커밋합니다 (제외하지 않음) ──
# migrations/ 는 팀이 공유해야 하므로 gitignore 하지 않습니다.