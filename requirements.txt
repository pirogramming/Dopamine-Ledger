# ── 코어 ──
Django>=5.0,<5.2
gunicorn>=21.0

# ── DB ──
psycopg[binary]>=3.1        # PostgreSQL 드라이버

# ── 인증 (소셜 로그인) ──
django-allauth>=0.61

# ── 이미지 (공유 카드) ──
Pillow>=10.0

# ── 환경변수 관리 ──
python-dotenv>=1.0
dj-database-url>=2.1

# ── 인증 (소셜 로그인) ──
django-allauth>=0.61
requests>=2.31
