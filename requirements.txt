# ── 코어 (필수) ──
Django==5.2.4
asgiref==3.9.1              # Django 의존성
sqlparse==0.5.3            # Django 의존성
tzdata==2026.2             # Windows 타임존 처리

# ── 설정/환경변수 (settings.py에서 사용) ──
python-dotenv==1.0.1       # .env 로드 (from dotenv import load_dotenv)
dj-database-url==2.3.0     # DATABASE_URL 파싱

# ── 소셜 로그인 (allauth) ──
django-allauth==65.3.0

# ── DB (배포: PostgreSQL) ──
psycopg2-binary==2.9.12
gunicorn

# ── 이미지 처리 (공유 카드 생성 - Pillow) ──
pillow==12.0.0

# ── HTTP 요청 (카카오 API 연동 시) ──
requests==2.34.2
